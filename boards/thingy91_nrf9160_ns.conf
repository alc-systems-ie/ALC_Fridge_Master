# Thingy:91 specific configuration (non-secure build with TF-M).

# Disable TF-M UART logging - it conflicts with I2C (same peripheral base address).
CONFIG_TFM_SECURE_UART=n
CONFIG_TFM_LOG_LEVEL_SILENCE=y

# Thingy:91 uses LTE modem, no BLE.
CONFIG_BT=n
CONFIG_BT_PERIPHERAL=n
CONFIG_BT_NUS=n
